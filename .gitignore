# ===== .NET / ASP.NET Core =====
bin/
obj/
*.user
.vs/
TestResults/
coverage/
*.log
logs/

# ===== Node / Vite (frontend) =====
node_modules/
dist/
dist_build/
dist-ssr/
dist.del.*
dist.trash
dist.bak.*
dist.old.*
.verify-dist/
.vd-*/
*.local
.vite/
.verify-dist/
.vite-cache/
*.tsbuildinfo
# vitest config loader temp artifacts
vitest.config.ts.timestamp-*.mjs
.tmp/
_tmp_*
.trash_*.bak

# ===== Secrets / credentials (NEVER commit) =====
# RSA private key used to sign JWT — leaking this defeats all auth
backend/CoupleLoveSystem.Api/keys/
secrets/
*.pem
*.key
.env
!.env.example
# Docker bind-mounted runtime data (volumes)
data/

# Real appsettings holds DB + SMTP passwords; keep a sanitized example instead
# NOTE: appsettings.Development.json is intentionally tracked — it contains NO secrets
# (only local Redis host + Seed flag) and carries the local-dev fix for the redis:6379 crash.
backend/CoupleLoveSystem.Api/appsettings.json

# Plaintext DB credential note kept locally for reference — never commit
MySQL账号密码.txt

# Self-signed localhost TLS cert/key for local HTTPS (regenerate with openssl, not a real secret but keep out of repo)
certs/

# ===== OS / editor =====
.DS_Store
Thumbs.db
.idea/
.vscode/
*.bak
*.bak/
*.bak.*

# ===== Python (if any scripts land in repo) =====
__pycache__/
*.pyc
# Temporary style-extraction helper (not part of the app)
extract_styles.py
